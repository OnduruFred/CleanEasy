% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_barplot.R
\name{gg_bar}
\alias{gg_bar}
\title{Flexible Bar Plot Wrapper for ggplot2 extensions.}
\usage{
gg_bar(
  data,
  x,
  grp_var = NULL,
  alpha = 1,
  color = "grey30",
  lable_size = 3.2,
  fill_n = "steelblue4",
  base_size = 11,
  orientation = "v",
  legend_pos = "right",
  palette = 5,
  ...
)
}
\arguments{
\item{data}{Dataset (data frame or tibble)}

\item{x}{A categorical variable name to appear on the x axis (must be non-numeric)}

\item{grp_var}{Optional grouping variable for stacked/grouped bars (must be non-numeric)}

\item{alpha}{Controls transparency of bars (0 to 1)}

\item{color}{Color for bar borders}

\item{lable_size}{Size for value labels}

\item{fill_n}{Default fill color when no grouping variable is specified}

\item{base_size}{Base font size for the plot}

\item{orientation}{Plot orientation ("v" for vertical or "h" for horizontal)}

\item{legend_pos}{Position of legend ("right", "left", "top", "bottom", or "none")}

\item{palette}{Color palette number (for grouped plots)}

\item{...}{Additional arguments passed}
}
\description{
The gg_bar() function is a custom wrapper around ggplot2 that generates a bar plot with optional features such as grouping variables, fill colors, labels, and orientation. It is designed to be flexible and user-friendly for categorical data visualization.
}
\examples{
\dontrun{
# Basic bar plot
gg_bar(mtcars, x = cyl)

# Grouped bar plot
gg_bar(mtcars, x = cyl, grp_var = am)

# Horizontal orientation
gg_bar(mtcars, x = cyl, orientation = "h")
}
}
